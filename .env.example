# ========= Trader 2.0 Environment =========
# Copy this to ".env" in the project root and edit MAP_* lines for YOUR broker.
# Canonical symbols must be these six (recommended set for live):
SYMBOLS=XAUUSD,US30USD,NAS100USD,SPX500USD,BTCUSD,WTICOUSD

# --- Optional broker symbol mappings (examples) ---
# Map CANONICAL -> BROKER SYMBOL actually shown in your MT5 MarketWatch
# (Uncomment and set to match your broker feed; leave commented if your broker uses the same names.)
# MAP_XAUUSD=XAUUSD
# MAP_US30USD=US30Cash
# MAP_NAS100USD=NAS100Cash
# MAP_SPX500USD=US500
# MAP_BTCUSD=BTCUSD
# MAP_WTICOUSD=WTICOUSD

# --- MT5 login (optional here; your existing diag may handle it) ---
# MT5_LOGIN=1600038177
# MT5_PASSWORD=********
# MT5_SERVER=OANDA-Demo-1

# --- Risk (used elsewhere; included here for completeness) ---
# FTMO_DAILY_LOSS=-5000
# FTMO_MAX_DRAWDOWN=-10000
# POSITION_SIZING=ATR_RISK         # or FIXED_LOT
# ATR_RISK_PCT=0.50                # percent of equity per trade when using ATR_RISK

# --- AI / Phases (future steps; harmless to keep here) ---
# ML_XGB=0                         # 0 = off (fallback to indicators), 1 = on
# PHASE_TARGETS=75,150             # trade count thresholds between phases
# MIN_CONF_PHASES=0.01,0.05,0.10   # per-phase min confidence gates
# COOLDOWN_PHASES=60,90,120        # seconds per phase
# MAX_POS_PHASES=5,4,3             # max concurrent positions per phase